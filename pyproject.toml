[project]
name = "herogold"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
backup = [
    "backup",
]
color = [
    "color",
]
config = [
    "config",
]
example = [
    "examples",
]
log = [
    "log",
]
progress = [
    "progress",
]
rainbow = [
    "rainbow",
]
sentinel = [
    "sentinel",
]
tree = [
    "tree",
]

# Convenience extras: install all optional groups
all = [
    "backup",
    "color",
    "config",
    "examples",
    "log",
    "progress",
    "rainbow",
    "sentinel",
    "tree",
]

[tool.uv.workspace]
members = [
    "herogold/*",
]

[tool.uv.sources]
backup = { workspace = true }
color = { workspace = true }
config = { workspace = true }
examples = { workspace = true }
log = { workspace = true }
progress = { workspace = true }
rainbow = { workspace = true }
sentinel = { workspace = true }
tree = { workspace = true }
